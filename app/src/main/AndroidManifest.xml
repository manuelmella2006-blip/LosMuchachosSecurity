<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos (si de verdad los necesitas) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LosMuchachosSecurity">

        <!-- FileProvider para compartir PDF -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- AdminActivity usando tu mismo tema -->
        <activity
            android:name=".ui.AdminActivity"
            android:exported="false"
            android:label="Modo Administrador"
            android:theme="@style/Theme.LosMuchachosSecurity" />

        <activity
            android:name=".ui.ControlActivity"
            android:exported="false" />

        <activity
            android:name=".ui.HistorialActivity"
            android:exported="false" />

        <activity
            android:name=".ui.Maqueta3DActivity"
            android:exported="false" />

        <!-- Login: pantalla de inicio -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main: panel principal -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true" />
    </application>

</manifest>
