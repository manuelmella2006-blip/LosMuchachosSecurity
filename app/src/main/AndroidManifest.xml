<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LosMuchachosSecurity"
        tools:targetApi="31">

        <!-- FileProvider para compartir PDFs -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- LoginActivity - Pantalla inicial -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivityUsuario - menú principal para usuarios -->
        <activity
            android:name=".ui.usuario.MainActivityUsuario"
            android:exported="false" />

        <!-- MainActivityAdmin - menú principal para administradores -->
        <activity
            android:name=".ui.admin.MainActivityAdmin"
            android:exported="false" />

        <!-- GestionarUsuariosActivity - CRUD de usuarios -->
        <activity
            android:name=".ui.admin.GestionarUsuariosActivity"
            android:exported="false"
            android:theme="@style/Theme.LosMuchachosSecurity"
            android:parentActivityName=".ui.admin.MainActivityAdmin">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.admin.MainActivityAdmin" />
        </activity>

        <!-- Maqueta3DActivity (opcional) -->
        <activity
            android:name=".ui.Maqueta3DActivity"
            android:exported="false" />

    </application>
</manifest>
